Phase1:(Translation)
pip install langchain-google-genai python-dotenv

Phase2: 
pip install fastapi uvicorn pydantic

To run the uvicorn/ app.py:
uvicorn app:app --reload

# OR
To run the uvicorn/ app1.py:
uvicorn app1:app --reload

# Only if you're running locally in your system make use of ngrok (https://ngrok.com/)
Run ngrok [bash] with: ngrok http 8000 

# The basic exposure to what this prototype/ project is?, will be given by "index.html"
File "index.html" will require uvicorn (line 8/12) and ngrok (line 15) codes to run.